# systemd drop-in installed at:
#   /etc/systemd/system/sovereign-explorer.service.d/metrics.conf
#
# The referenced file is deployment-owned, mode 0600, and contains:
#   METRICS_TOKEN=<high-entropy bearer token>

[Service]
EnvironmentFile=-/etc/sov-explorer.metrics.env
